name: esc_pos_bluetooth_forked
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer. Forked by ToanNM
version: 1.0.7
homepage: https://github.com/toannmdev/esc_pos_bluetooth

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.25.0
  esc_pos_utils: ^1.0.0
  # esc_pos_utils:
    # path: ../esc_pos_utils
  flutter_bluetooth_basic:
    git: https://github.com/iceskyboy/flutter_bluetooth_basic.git
  image: ^2.1.19

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
